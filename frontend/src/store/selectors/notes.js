export const notesListSelector = state => state.note.list;
export const noteAddSelector = state => state.note.add;

export const noteLoadingSelector = state => state.note.loading;
export const noteErrorSelector = state => state.note.error;
